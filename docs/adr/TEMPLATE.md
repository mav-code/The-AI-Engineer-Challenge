# ADR-00NN: <short imperative title>

- **Status:** Accepted | Superseded by ADR-00NN
- **Decided:** YYYY-MM-DD (`<commit>`)
- **Recorded:** YYYY-MM-DD
- **Applies to:** <files, directories, or the situation this governs>

## Context

What forced a choice. The constraints, and what makes the obvious alternative
wrong here. Enough that someone can judge whether the constraints still hold.

## Decision

The rule, stated so it can be followed without reading the rest. Bold the parts
that are load-bearing.

## Consequences

What this costs, what it forbids, and the known upgrade path if the constraints
change. Include how to verify the decision is still honoured, where that's
possible.
